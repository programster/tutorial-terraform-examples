This example deploys an S3 bucket with:

* Default AES256 encryption (no need to use KMS)
* Versioning
* Private access control list (objects aren't public).
* Prevents objects from being able to be made public.
